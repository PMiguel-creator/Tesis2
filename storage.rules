rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Solo el usuario propietario puede leer, escribir y eliminar sus archivos
    match /documents/{userId}/{allPaths=**} {
      allow read, write, delete: if request.auth != null && request.auth.uid == userId;
    }
  }
}
